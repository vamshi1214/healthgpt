[project]
name = "logging-server"
version = "0.1.0"
description = "Solar logging server template"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.12",
    "requests>=2.32.3",
    "uvicorn>=0.34.1",
    "websockets>=15.0.1",
]
